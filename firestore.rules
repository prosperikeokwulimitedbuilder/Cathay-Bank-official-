rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All privileged data access is performed by the authenticated server.
    // The browser must not be able to bypass server-side authorization.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
